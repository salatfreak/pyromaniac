[project]
name = "pyromaniac"
version = "0.4.0"
description = "Obsessed with creating Ignition"
readme = "README.md"
license = { file = "LICENSE" }
authors = [{ name = "salatfreak" }]

dependencies = [
  "PyYAML ~= 6.0.2",
  "Jinja2 ~= 3.1.5",
  "cryptography ~= 44.0.0",
]

[tool.setuptools]
packages = ["pyromaniac"]

[tool.ruff.lint]
select = ["F", "E", "W"]
preview = true
