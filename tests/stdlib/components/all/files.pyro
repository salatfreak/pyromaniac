---
storage = std.storage(
    std.tree("/tree", _/"tree"),
    std.file("/foo.txt", "foo", "core"),
    *std.directories("/", "foo/bar"),
    std.link("/link.txt", "/foo.txt"),
)
---

storage: `storage`